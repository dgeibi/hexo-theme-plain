extends _base.pug

block content
  article.post
    header.post-header
      h1.post-title(itemprop="name headline") #{page.title}
    section.post-content.toc-src(itemprop="articleBody")
      block subcontent
